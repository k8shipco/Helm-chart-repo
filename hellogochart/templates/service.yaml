apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-hellogo
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.image.name }}
  ports:
  - port: {{.Values.port}}
    targetPort: {{.Values.port}}
    protocol: TCP
    name: http